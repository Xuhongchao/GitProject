package com.pxene.test;

import org.junit.Test;

public class UtilTest {
	@Test
	public void byteArray2Long() throws Exception {
		byte[] b = new byte[] { '1', 'V', '3', 'a' };
		int value = 0;
		for (int i = 0; i < 4; i++) {
			int shift = (8 - 1 - i) * 8;
			value += (b[i] & 0x000000FF) << shift;
			System.out.println(value);
		}
	}
}
